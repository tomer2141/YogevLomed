function init(){
    var GAME_TEMPLATE = document.getElementById("game-template");
    //Start
    var rows = 3;
    var cols = 3;

    for(var i = 0; i < rows; i++){
        var row = [];
        for(var j = 0; j < cols; j++){
            var cell = {};
            
        }
    }
}